name: CarbonERP V17 Collection (legacy manual)
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'V17 legacy workflow is manual-only. Current application is maintained by V20.'
